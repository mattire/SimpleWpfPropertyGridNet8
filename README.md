Usage:

            var window = new PropertyGridWindow(yourObject, "test")
            {
                Owner = parentWindow
            };
            window.Show();

This repository contains .Net 8 libraries for .Net 9 checkout SimpleWpfPropertyGrid repository.
